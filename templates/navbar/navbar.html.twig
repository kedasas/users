<nav class="navbar navbar-expand-sm bg-light navbar-light">
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" href="{{ path('users_list') }}">Users List</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('group_list') }}">Groups List</a>
        </li>
        {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link " href="{{ path('new_user') }}">Add user</a>
            </li>
        {% endif %}
        {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link " href="{{ path('new_group') }}">Add group</a>
            </li>
        {% endif %}
    </ul>
    {% if is_granted('ROLE_USER') %}
        <ul class="navbar-nav ml-auto">
            <li class="nav-item dropdown">
                <a class="nav-link" href="#" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{ app.user.getName() }} <i class="fa fa-user"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="dropdown-item" href="/users/edit/{{ app.user.id }}">Edit profile</a>
                    {% endif %}
                    <a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a>
                </div>
            </li>
        </ul>

    {% endif %}

</nav>